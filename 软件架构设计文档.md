# Glance 软件架构设计文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档状态 | 架构基线草案，等待评审 |
| 目标产品 | Glance 一瞥 |
| 目标平台 | Windows 11 x64 |
| 主要语言 | C++20 |
| 主要工具链 | Visual Studio、MSVC、Windows 11 SDK、MSBuild |
| UI 技术 | WinUI 3、Windows App SDK、Win32 HWND 互操作 |
| 设计优先级 | 输入可靠性 > 行为正确性 > 响应延迟 > 资源占用 > 功能覆盖 |
| 需求来源 | `需求文档.md` |

本文档定义 Glance 的架构边界、关键技术决策、模块职责、状态模型、可靠性策略和实施阶段。本文档不替代产品需求；当本文档与需求文档冲突时，应先明确并修订需求，再修改架构基线。

## 2. 设计目标

### 2.1 核心目标

1. 在受支持的文件选择窗口中可靠捕获 Space/Esc，并严格限制触发上下文。
2. 默认预览窗口不抢占来源窗口焦点，方向键等输入继续由来源窗口处理。
3. 文件选择变化后取消过期加载，快速切换到最新文件，不显示乱序结果。
4. 支持一个活动预览和多个已分离持续预览窗口。
5. 对不可信文件解析、Office 自动化和 Web 内容进行故障与安全隔离。
6. 常驻核心保持低 CPU、低内存、无轮询或仅在必要场景短时自适应轮询。
7. 新增预览格式时只新增编译期模块，不引入运行时插件发现和脚本运行环境。

### 2.2 非目标

1. 不支持任意第三方文件管理器；仅支持经过适配和版本验证的宿主。
2. 不提供编辑、保存、解压、Office 文档修改或媒体转码功能。
3. 不保证在安全桌面、登录桌面、锁屏桌面及系统完整性级别窗口中工作。
4. 不为非文件系统 Shell 项目自动创建本地副本。
5. 不实现 Python、JavaScript 扩展目录、动态 DLL 扫描等运行时插件机制。
6. 不依赖系统 PATH 中版本不确定的外部程序；FFmpeg 等媒体依赖必须固定版本并随应用受控部署。

### 2.3 需求追踪

| 需求域 | 主要架构落点 | 关键验证阶段 |
| --- | --- | --- |
| Space/Esc 可靠捕获 | Core、双 Hook、Raw Input、InteractionSnapshot | 阶段 0、1 |
| Explorer/文件对话框/Everything/Listary | Selection Adapter | 阶段 0、1、5 |
| 不抢焦点并跟随选择 | 窗口状态机、WinEvent、WinUI/Win32 互操作 | 阶段 0、1 |
| 置顶、钉住、多实例 | Window Session、实例注册和 IPC | 阶段 3 |
| 窗口缩放和内容自适应 | WindowSizingService、Provider 建议尺寸 | 阶段 1、2 |
| 文本、代码、Markdown | Text/Markdown Provider | 阶段 2 |
| 图片和 EXIF | Image Provider | 阶段 2 |
| 音视频 | Media Provider | 阶段 2 |
| PDF、压缩包、Office | 隔离 Provider Host | 阶段 4 |
| 格式猜测和通用文件 | Probe Pipeline、Generic Provider | 阶段 1、2 |
| 虚拟文件和云占位文件 | FileDescriptor、一次确认、普通文件加载 | 阶段 1、5 |
| 暗色模式、强调色和设置 | WinUI Shell、Settings Store | 阶段 1、2 |
| 管理员权限与拒绝降级 | 提权 Core、普通权限 UI、受限 IPC | 阶段 0、1 |

## 3. 关键架构决策

### ADR-001：采用 C++20 原生实现

**状态：已决定。**

使用 C++20 统一实现 Win32、COM、UI Automation、C++/WinRT、DirectX 和 Media Foundation 集成。避免在 Hook 关键路径引入托管运行时、垃圾回收暂停或跨语言回调。

### ADR-002：核心单实例，UI 单进程多窗口，危险能力按需隔离

**状态：已决定。**

基础运行时至少包含一个集成核心进程和一个 UI 进程。UI 进程承载一个活动预览窗口和任意数量的独立预览窗口。Office、PDF、压缩包等后端可以按风险和资源成本进入按需宿主进程。

“预览窗口多实例”不等于每个窗口固定创建一个进程。普通窗口共享 UI 进程和图形设备；只有解析风险、COM 生命周期或崩溃影响需要隔离时才创建宿主进程。

### ADR-003：仅集成核心申请管理员权限

**状态：已决定，需原型验证 Shell COM 跨完整性级别行为。**

`Glance.Core.exe` 通过 `runas` 请求提升，`Glance.exe` 保持调用用户的普通权限。这样可以：

- 让 Hook 和 UI Automation 覆盖普通及大部分高权限文件选择窗口；
- 防止 WebView2、Office COM 和外部默认程序无意继承管理员权限；
- 将高权限代码限制在不解析文件、不渲染 Web 内容的最小进程中。

用户拒绝 UAC 时，核心以普通权限启动，UI 显示持久但非阻塞警告，并明确高权限窗口不受保证。

### ADR-004：低级键盘 Hook 是拦截主路径，Raw Input 是健康监控路径

**状态：已决定。**

`WH_KEYBOARD_LL` 负责捕获和按条件吞掉 Space/Esc。Raw Input 不参与拦截，仅用于交叉检查物理按键是否被 Hook 线程观察到，以及触发 Hook 重建。低级 Hook 吞键后 Raw Input 的具体可见性必须在阶段 0 实测；若目标按键不可见，则使用非目标按键流和 Hook 轮换心跳评估健康状态。

### ADR-005：WinUI 3 负责应用外壳，原生渲染器负责内容

**状态：需原型验证。**

WinUI 3 用于标题栏、底栏、文件列表、设置页和标准控件。图片、文本、媒体等内容区域使用原生 HWND 或 DirectComposition 表面。必须在第一阶段验证 `WS_EX_NOACTIVATE`、`WM_MOUSEACTIVATE`、WinUI 指针事件、Scintilla 子窗口及 WebView2 组合托管能否同时满足不抢焦点要求。

如果 WinUI 3 无法稳定满足非激活窗口输入和原生子窗口组合要求，主预览窗口降级为 Win32 + DirectComposition 自绘，设置窗口仍使用 WinUI 3。该降级不改变业务模块接口。

### ADR-006：使用固定、可审计的第三方源码依赖

**状态：已决定。**

允许 Scintilla/Lexilla、PDFium、libarchive、Markdown 解析器和 FFmpeg/libav* 等确定性依赖。所有版本固定、随源码或锁定包构建，不允许运行时发现未知模块。

### ADR-007：首版不引入 Windows 系统服务

**状态：已决定。**

Windows 服务运行于非交互 Session 0，不能直接捕获当前用户桌面的键盘输入，也不能替代用户会话内的 UI Automation、Shell COM 和窗口监控。引入服务仍需额外启动每用户 Session Agent，并增加 LocalSystem/高权限 IPC、安装、升级和多会话安全面，因此不会提高 Hook 关键路径的可靠性。

首版使用用户会话内的 Core、双 Hook、UI/Core 心跳和自动重建。安装模式下优先评估“仅在指定用户登录时运行、最高权限”的计划任务启动 Core，并配置有界失败重启；它仍需在阶段 0 验证交互桌面、登录/解锁和多会话行为。计划任务不可用时，由 UI 使用 `runas` 启动 Core。Windows 服务未来只能作为可选的更新、安装修复或企业部署组件，不得参与按键判定、选择读取和文件预览。

### ADR-008：媒体后端按性能与兼容性自适应选择

**状态：已决定，需基准验证。**

媒体 Provider 同时抽象 Media Foundation 和 FFmpeg/libav* 后端，不预设其中一个永久为主后端。根据容器、编解码器、硬件解码能力、首帧延迟、Seek 延迟、GPU/CPU 占用、功耗和稳定性选择后端；首选后端打开或解码失败时，另一个后端作为降级。

发布版本直接集成固定版本的 libavformat、libavcodec、libavutil、libswresample 等库，不通过 PATH 启动 `ffmpeg.exe`。命令行进程会引入启动、管道传输和原始帧复制成本，仅用于开发期探测和对照实验。

## 4. 系统上下文

```text
                       +-----------------------+
                       | Explorer / File Dialog|
                       | Everything / Listary  |
                       +-----------+-----------+
                                   |
                    WinEvent / Shell COM / UIA
                                   |
+----------+       Named Pipe      v       Hook / Raw Input
| Glance UI| <----------------> Glance Core <-------------- Keyboard
|  medium  |                    elevated or medium fallback
+----+-----+
     |
     +---- Direct2D/WIC/Media Foundation/FFmpeg/Scintilla
     +---- WebView2 runtime processes
     +---- Glance.RenderHost.exe (on demand)
     +---- Glance.OfficeHost.exe (on demand) ---- Office COM
```

## 5. 进程模型

### 5.1 Glance.exe

权限：普通用户权限。生命周期：用户会话内单实例。

职责：

- 启动、连接和监督 `Glance.Core.exe`；
- 管理活动预览和独立预览窗口；
- 维护窗口状态机、布局、主题和用户设置；
- 调度预览 Provider，取消过期请求；
- 管理共享 D3D11/DirectComposition 设备；
- 启动按需 RenderHost、OfficeHost 和 WebView2 环境；
- 执行复制路径、打开所在文件夹和默认程序打开等普通权限操作。

禁止：

- 安装全局 Hook；
- 以管理员身份启动默认程序或 Office；
- 在 UI 线程同步读取、探测或解析文件。

### 5.2 Glance.Core.exe

权限：优先管理员权限，UAC 被拒绝时普通权限。生命周期：用户会话内单实例。

职责：

- 安装和维护低级键盘 Hook；
- 注册 Raw Input 和系统会话/电源事件；
- 追踪前台窗口、焦点控件和宿主身份；
- 运行 Explorer、文件对话框、Everything、Listary 选择适配器；
- 维护只读、原子化的 `InteractionSnapshot`；
- 决定 Space/Esc 是否吞键，并向 UI 发送预览命令；
- 对 UI 进程进行身份校验和 IPC 限权。

禁止：

- 解析或渲染用户文件；
- 加载 WebView2、PDFium、libarchive 或 Office COM；
- 代表 UI 执行任意路径访问、进程启动或文件写入；
- 在 Hook 回调中执行 COM、IPC、日志、锁等待或堆分配。

### 5.3 Glance.RenderHost.exe

权限：普通用户权限。生命周期：按需启动，可复用，空闲回收。

首期用于 PDF 和压缩包解析；后续可承载不稳定的图片编解码器。宿主只接收受限的只读文件句柄或已验证路径，不接收任意命令。解析结果通过共享内存、只读缓存文件或受限 IPC 返回。

### 5.4 Glance.OfficeHost.exe

权限：普通用户权限。生命周期：按需启动，单 STA COM 线程。

职责是以只读、禁用宏、禁止交互提示的方式调用本机 Office，将受支持文档导出为临时 PDF 或固定布局预览数据。OfficeHost 不复用用户已打开的 Office 实例，不终止无法确认归属的 Office 进程。

### 5.5 WebView2 子进程

由 Evergreen WebView2 Runtime 管理。Glance 共享一个用户数据目录和环境，但每个需要 Web 内容的窗口使用独立 Controller。Markdown 页面仅加载随应用发布的资源；默认阻止网络访问、新窗口、下载、权限请求和外部导航。

## 6. 线程模型

### 6.1 Core 线程

| 线程 | 职责 | 约束 |
| --- | --- | --- |
| Hook Primary | 主低级键盘 Hook 和消息循环 | 回调 O(1)，无锁、无分配 |
| Hook Standby | 冗余 Hook 和独立消息循环 | 主 Hook 失效时继续工作 |
| Input Monitor | Raw Input、Hook 健康关联 | 不参与吞键决策 |
| Window Observer | WinEvent、前台窗口和焦点变化 | 只发布宿主变化事件 |
| Selection STA | Shell COM 和需要 STA 的适配器 | 不得阻塞 Hook |
| UIA MTA | UI Automation 查询和事件 | 超时、代次隔离 |
| IPC | 命名管道收发和心跳 | 有界队列、消息限长 |

主 Hook 最后安装，使其位于 Hook 链前部；Standby 保持已安装状态。主 Hook 在符合条件时吞键，Standby 不会收到该事件；主 Hook 被系统静默移除后，Standby 仍可接管。系统恢复、会话解锁、Explorer 重启和健康异常时执行无窗口期轮换：先安装新 Hook，再卸载旧 Hook。

### 6.2 UI 线程

| 线程 | 职责 |
| --- | --- |
| UI STA | WinUI、窗口状态、轻量渲染提交 |
| Preview Dispatcher | Provider 探测、任务调度、取消和结果合并 |
| File I/O Pool | 文件头、属性、文本分块和缩略图读取 |
| Media callbacks | Media Foundation/FFmpeg 后端事件、音视频时钟 |
| Graphics thread | 需要时负责 D3D/DirectComposition 提交 |
| IPC | 与 Core 和隔离宿主通信 |

任何异步结果必须携带 `requestId` 和 `generation`。只有与当前窗口代次一致的结果可以提交到 UI；旧结果直接释放，不能造成预览回跳。

## 7. 输入捕获与可靠性设计

### 7.1 Hook 决策快照

Hook 只读取不可变的 `InteractionSnapshot`：

```text
foregroundHwnd
foregroundPid
foregroundIntegrity
hostKind
focusIsFileList
selectionCount
selectionGeneration
activePreviewState
coreMode
snapshotTimestamp
```

快照通过双缓冲或原子指针发布。Hook 回调不读取选中文件路径；路径解析发生在选择线程。

### 7.2 Space 处理

1. 忽略 Core 自身明确标记的注入输入；其他注入输入暂按正常按键处理，以兼容键盘映射软件。
2. 忽略自动重复产生的后续 KeyDown，但持续吞掉已接管按键序列的 KeyUp。
3. 若存在活动预览，Space 关闭活动预览，不影响独立实例。
4. 若不存在活动预览，仅当快照表明前台是受支持文件列表且选择非空时吞键。
5. Hook 将固定大小事件写入预分配 SPSC 队列并立即返回。
6. Core 重新获取或确认最新选择，向 UI 发送 `OpenActivePreview`。
7. UI 立即显示轻量窗口骨架，内容异步加载。

### 7.3 Esc 处理

仅当存在活动预览时接管 Esc。独立实例不响应全局 Esc。关闭活动预览后，必须更新 Core 快照，再放行后续 Esc。

### 7.4 可靠性边界

Windows 用户态 Hook 无法给出数学意义的“永不丢失”保证。以下场景明确排除：安全桌面、会话切换瞬间、系统挂起/恢复窗口、Core 被强制终止、操作系统拒绝 Hook，以及系统完整性级别进程。

工程目标是通过双 Hook、专用线程、O(1) 回调、Raw Input 交叉检查、心跳、自动重建和恢复事件处理，将可控制原因导致的失效降至最低，并使失效可检测、可记录、可恢复。

选择状态同样存在时序边界。UI Automation/Shell 选择变化事件可能晚于用户的下一次 Space，Hook 回调又不能同步执行 COM 查询。Core 应在方向键、鼠标选择事件和焦点变化后立即提高选择刷新优先级，并为快照记录年龄和 generation；阶段 0 必须测量最坏延迟。快照过旧时的首期策略是放行 Space 而不是基于旧选择打开错误文件，具体年龄阈值由实测确定。

## 8. 文件选择适配器

### 8.1 统一接口

```cpp
class ISelectionAdapter {
public:
    virtual HostMatch Match(const ForegroundContext& context) = 0;
    virtual SelectionSnapshot QuerySelection(const QueryContext& context) = 0;
    virtual Subscription Subscribe(SelectionChangedCallback callback) = 0;
};
```

实际接口使用 `std::expected` 风格的明确错误类型，并包含超时、宿主已销毁、访问拒绝、控件树变化等状态。Provider 和适配器均静态注册，禁止运行时加载外部实现。

### 8.2 Explorer 适配器

优先路径：Shell COM，获取当前标签页对应的 Shell View、选中项和 focused item。UI Automation 仅作为降级路径。必须覆盖：

- Windows 11 Explorer 标签页；
- 桌面窗口；
- 搜索结果；
- 详细信息、图标和列表视图；
- Explorer 重启和窗口句柄复用。

### 8.3 原生文件对话框适配器

使用 UI Automation 原生接口识别文件列表控件，并通过 Selection Pattern 获取选择。必须区分文件列表、文件名输入框、搜索框和对话框按钮，防止用户在输入空格时误触发预览。

### 8.4 Everything/Listary 适配器

按进程签名、可执行文件路径、窗口类和 UIA 控件结构组合识别。每个支持版本拥有明确的适配器版本和兼容矩阵；控件树未知时默认不接管 Space。

### 8.5 选择顺序

操作系统通常只能稳定提供视图顺序和 focused item，不能恢复应用启动前的用户点击时间顺序。架构将“原生顺序”定义为适配器返回的视图顺序，默认项定义为 focused item。运行期间可记录选择事件，但不将推测出的点击顺序作为稳定接口。

## 9. 文件描述模型

`FileDescriptor` 至少包含：

```text
displayName
filesystemPath
shellParsingName
extension
size
creationTime
lastWriteTime
attributes
volumeId + fileId
isFilesystem
isCloudPlaceholder
isHydrated
sourceHwnd + sourcePid + sourceKind
```

文件身份优先使用卷标识和 File ID，路径仅用于显示和重新打开。打开文件后记录最终路径、大小和修改时间，避免路径重定向及选择变化造成 TOCTOU 混淆。

非文件系统 Shell 项目直接进入 Generic Provider。虚拟文件和云占位文件不自动触发下载；用户点击“尝试普通文件载入”并完成一次确认后，按普通文件执行只读打开。载入失败直接返回 Provider 错误，不实现下载进度、云服务 API 或其他专用适配。

当提权 Core 能读取选择但普通权限 UI 无权打开文件时，Core 不得直接接受 UI 提供的任意路径并代为读取。阶段 0 需要评估两种受限方案：由 Core 打开已由 Selection Adapter 产生且身份一致的只读句柄并定向复制到已认证 UI 进程，或为该请求启动最小权限的专用读取代理。方案确定前，此场景降级为显示基础信息和访问受限提示。

## 10. 预览窗口状态机

### 10.1 状态

| 状态 | 跟随选择 | 因来源失焦关闭 | 响应全局 Space/Esc | 占用活动名额 |
| --- | --- | --- | --- | --- |
| Hidden | 否 | 否 | Space 可创建 | 否 |
| ActiveFollowing | 是 | 是 | 是 | 是 |
| ActiveTopmost | 是 | 否 | 是 | 是 |
| ActivePinned | 否 | 否 | 是 | 是 |
| DetachedPinnedTopmost | 否 | 否 | 否 | 否 |
| DetachedUnpinned | 否 | 下一次前台焦点变化 | 否 | 否 |

### 10.2 关键转换

```text
Hidden --Space(valid selection)--> ActiveFollowing
ActiveFollowing --Space/Esc/source blur--> Hidden
ActiveFollowing --topmost on--> ActiveTopmost
ActiveFollowing --pin on--> ActivePinned
ActiveTopmost --pin on--> DetachedPinnedTopmost
ActivePinned --topmost on--> DetachedPinnedTopmost
DetachedPinnedTopmost --unpin--> DetachedUnpinned
DetachedPinnedTopmost --close button--> Closed
DetachedUnpinned --foreground change/close button--> Closed
```

分离时冻结当前选择列表、当前项、渲染状态、窗口位置和尺寸，并生成新的 `instanceId`。Core 收到 `Detached` 确认后释放活动名额，随后 Space 可以创建新的活动预览。独立实例不设置数量上限，但共享缓存、GPU 资源和空闲 Provider 仍执行统一回收。

### 10.3 焦点模型

活动窗口使用非激活显示，不成为前台窗口。来源窗口句柄保存在会话中；其前台资格丢失时，根据状态决定关闭或继续。独立窗口不再依赖来源窗口。

鼠标点击标题栏按钮、底栏按钮、滚轮和内容拖动不得自动激活活动窗口。对于必须获得键盘焦点的 Web 控件功能，首期不开放或要求显式进入交互模式；不得隐式破坏“方向键属于来源窗口”的规则。

## 11. 窗口尺寸与布局

### 11.1 初始尺寸计算

`WindowSizingService` 输入内容固有尺寸、Provider 建议尺寸、DPI、工作区、文件列表宽度及窗口装饰尺寸，输出逻辑像素窗口矩形。

规则：

1. 初始尺寸限制在当前显示器工作区内，并保留可拖动标题栏。
2. 图片和视频按固有宽高比缩放到内容区域，不允许拉伸。
3. 小图不默认无限放大；可设置 1:1 和 Fit 两种建议策略。
4. 文本、PDF、Office、压缩包使用按 DPI 缩放的阅读尺寸。
5. 多选文件列表占用独立布局宽度，不能压缩内容区到最小值以下。
6. 窗口必须处理 Per-Monitor V2 DPI、显示器热插拔和工作区变化。

### 11.2 手动尺寸优先

窗口创建后处于 `AutoSized`。收到用户边框调整或最大化操作后进入 `UserSized`，后续文件切换不再改变外框尺寸。恢复自适应按钮重新计算当前内容的建议尺寸并返回 `AutoSized`。

按预览类型记忆尺寸是可选设置。只记录 DIP 尺寸和窗口状态，不记录绝对屏幕坐标；恢复时重新约束到当前工作区。

## 12. Preview Provider 架构

### 12.1 统一生命周期

```text
Probe -> Plan -> Open -> LoadAsync -> Present -> UpdateViewport -> Close
```

- `Probe` 只读取扩展名、文件属性和有界文件头；
- `Plan` 返回 Provider、风险等级、是否需要宿主进程及建议窗口尺寸；
- `Open` 建立只读会话；
- `LoadAsync` 可取消并允许分阶段返回元数据、缩略图和完整内容；
- `Present` 只在 generation 匹配时提交；
- `Close` 必须幂等，释放句柄、COM 对象和 GPU 资源。

### 12.2 Provider 选择顺序

1. 已知扩展名对应 Provider，并使用签名进行合理性校验。
2. 未知扩展名先进行 BOM、UTF-8 合法性、NUL 比例和可打印字符比例检查。
3. 若不像文本，再匹配有界媒体/文档 Magic Bytes。
4. 无匹配时进入 Generic Provider。

Provider 不依赖扩展名作为唯一安全依据。探测读取量默认不超过 64 KiB，禁止为判断格式扫描完整文件。

### 12.3 Provider 矩阵

| 类型 | 首选后端 | 隔离 | 主要能力 | 降级 |
| --- | --- | --- | --- | --- |
| 文本/代码 | Scintilla + Lexilla | UI 进程 | 行号、样式、虚拟滚动、缩放 | 纯文本只读视图 |
| Markdown Code | Scintilla + Lexilla | UI 进程 | 与代码预览一致 | 纯文本 |
| Markdown Preview | Markdown parser + WebView2 | WebView2 子进程 | 本地 HTML、主题、滚动 | Code 模式 |
| 图片 | WIC + Direct2D | UI 进程 | EXIF、多帧、缩放、旋转、平移 | Shell 缩略图 |
| 音视频 | 自适应 Media Foundation/FFmpeg | UI 进程，必要时 MediaHost | 硬件解码、播放、进度、音量、元数据 | 另一媒体后端/Generic |
| PDF | PDFium | RenderHost | 页面、缩略图、目录、渐进渲染 | WebView2/Generic |
| 压缩包 | libarchive | RenderHost | 只读目录树、属性、搜索 | Generic |
| Office | Office COM 导出 PDF | OfficeHost | 只读固定布局预览 | Generic/默认程序打开 |
| 通用文件 | Shell thumbnail/property system | UI 进程 | 缩略图、路径、大小、时间 | 基础图标和属性 |

## 13. 各 Provider 设计约束

### 13.1 文本与编码

- 读取 BOM 后优先确定 UTF-8/UTF-16；无 BOM 时先验证 UTF-8，再调用固定版本编码检测器；
- 检测结果必须显示置信度不足状态，用户手动选择编码后重新解码；
- 大文件使用内存映射或分块读取，不一次性生成多份完整 Unicode 副本；
- 文件只读，禁用编辑、撤销栈、自动完成和输入法写入；
- 语法高亮按可见区和邻近缓冲增量执行；
- 超大单行文件设置明确保护阈值，避免布局和 Lexer 长时间占用线程。

### 13.2 图片

- WIC 解码在后台线程进行，GPU 上传在图形上下文执行；
- 优先读取缩略图或按目标尺寸缩放解码，避免巨幅图片完整展开导致内存峰值；
- EXIF 原始值转换为统一显示模型，不在 UI 中直接暴露未处理 VARIANT；
- 最小缩放恒等于 Fit 比例，窗口变小时重新计算 Fit 下界；
- 旋转仅改变显示变换，不写回原文件。

### 13.3 媒体

- `IMediaBackend` 统一暴露 Open、Play、Pause、Seek、Track、Metadata、Volume 和异步错误接口，控制层不依赖具体解码器；
- Media Foundation 后端优先使用系统硬件解码和 D3D11 表面；FFmpeg 后端优先尝试 D3D11VA，并允许按设备能力选择 QSV、NVDEC 等路径；
- FFmpeg 视频帧应尽量保持为 D3D11 纹理并直接进入合成链，避免 GPU 到 CPU 再回 GPU 的复制；音频统一输出到共享播放时钟和 WASAPI 渲染层；
- 后端选择依据固定能力表和实测策略，不在每次打开文件时执行重型基准；设置中提供自动、Media Foundation、FFmpeg 三种诊断选项；
- 首选后端在打开或早期解码失败时，降级后端从相同时间点重新建立会话；持续播放中不承诺无缝切换；
- 打开新媒体前异步停止旧会话，禁止旧回调更新新窗口；
- 进度拖动合并高频输入，音量设置与静音状态分离；
- 无视频轨的音频显示封面或通用媒体视觉，不启动空白视频表面；
- 两个后端均失败时显示明确的格式、编解码器和硬件路径错误状态。

### 13.4 PDF

- PDFium 在 RenderHost 中打开不可信文件；
- 首屏优先渲染当前页，目录和缩略图按需加载；
- 页面位图按缩放级别、页码、文档 File ID 缓存；
- 密码 PDF 显示受保护且不支持状态，不提供会抢占焦点的密码输入；
- 限制同时渲染页数和总缓存，切换文件时立即取消排队页面。

### 13.5 压缩包

- 只列目录和元数据，不自动解压或执行内容；
- 限制最大条目数、目录深度、单条目声明大小、总展开大小和压缩比；
- 路径必须规范化并标记目录穿越名称，即使首期不落盘；
- 加密压缩包显示不支持状态，不在非激活窗口中请求密码；
- 嵌套压缩包不递归展开。

### 13.6 Office

- 仅支持 Word、Excel 和 PowerPoint，并在检测到对应 Office COM 注册后启用；不支持 Visio、Project 或其他 Office 产品；
- 文档以只读方式打开，禁用宏、链接更新、模板更新和交互提示；
- 导出文件存入当前用户私有临时目录，文件名不直接使用原始路径；
- 导出过程设软超时和硬超时，UI 可以取消；
- 仅退出由 OfficeHost 创建且可确认归属的 COM 实例；
- 不保证损坏文档、受密码保护文档、受保护视图和第三方 Office 加载项场景。

## 14. IPC 设计

### 14.1 传输

Core 与 UI 使用每用户命名管道：

```text
\\.\pipe\Glance.{UserSid}.Core.v1
```

管道 DACL 只允许当前交互用户和 SYSTEM。Core 在连接后校验客户端 PID、令牌用户 SID、会话 ID 和可执行文件路径。高权限 Core 不接受“读取任意文件”“启动任意程序”等可被低权限客户端利用的请求。

### 14.2 消息格式

使用固定二进制帧头和 UTF-8 JSON 载荷：

```text
magic | protocolVersion | messageType | flags | payloadLength | correlationId
```

JSON 由系统 WinRT JSON API 解析，避免额外序列化依赖。每类消息设置最大长度，文件列表采用总条目数和总字节数上限。协议版本不兼容时拒绝连接并提示组件版本不一致。

### 14.3 主要消息

- `Hello` / `HelloAck`
- `Heartbeat`
- `CoreHealthChanged`
- `ForegroundContextChanged`
- `SelectionChanged`
- `OpenActivePreview`
- `CloseActivePreview`
- `ActivePreviewStateChanged`
- `DetachPreview`
- `DetachedPreviewAck`
- `SettingsForCoreChanged`

Hook 回调不直接写命名管道。IPC 断开时 Core 默认放行按键，避免在 UI 不可用时吞掉用户输入。

## 15. 配置、缓存和日志

### 15.1 配置

路径：`%LOCALAPPDATA%\Glance\settings.json`。

配置包含 `schemaVersion`，使用临时文件、Flush、原子替换写入。无法解析时保留损坏副本并恢复默认值，不因配置损坏阻止 Hook 启动。Core 所需的少量设置由 UI 校验后通过 IPC 下发。

### 15.2 缓存

路径：`%LOCALAPPDATA%\Glance\Cache`。

缓存按 Provider 分区，使用 File ID、修改时间、文件大小和渲染参数组成 Key。实施总容量预算和 LRU 回收。Office 临时导出、PDF 缩略图和 WebView2 用户数据分别管理，敏感临时文件使用当前用户专用 ACL。

### 15.3 可观测性

- 使用 TraceLogging/ETW 记录 Hook 状态、适配器选择、请求代次、Provider 延迟和错误码；
- 默认日志不记录完整文件路径，可记录扩展名、宿主类型和散列化身份；
- 维护有界内存事件环，在崩溃或用户主动导出诊断时落盘；
- 关键健康指标包括 Hook 重建次数、Raw Input 不匹配、IPC 重连、选择查询超时和 Provider 崩溃。

## 16. 安全设计

1. Core 遵循最小功能原则，不解析用户控制的数据文件。
2. 所有文件以只读共享模式打开，不写入源文件。
3. 文件探测、图片尺寸、媒体帧、PDF 页面和压缩包条目都有硬限制。
4. WebView2 页面配置 CSP，禁用网络、下载、摄像头、麦克风、剪贴板写入及外部导航。
5. Markdown HTML 输出必须转义原始 HTML或经过白名单清理；首期默认禁用原始 HTML。
6. Office 自动化禁用宏和外部链接更新，临时导出与用户 Office 会话隔离。
7. 高权限 Core 的命名管道必须验证客户端，防止成为本地提权代理。
8. “默认程序打开”和“打开所在文件夹”由普通权限 UI 通过 Shell 执行。
9. RenderHost 崩溃不终止 Core；同一文件重复导致宿主崩溃时进入熔断并回退 Generic Provider。

## 17. 性能预算

以下为架构验收目标，需在代表性 Windows 11 设备上实测后调整：

| 指标 | 目标 |
| --- | --- |
| Hook 回调耗时 | P99 < 0.2 ms，任何路径 < 1 ms |
| 空闲 Core CPU | 60 秒平均 < 0.1% 单核 |
| 空闲 Core 工作集 | < 20 MiB |
| UI 基础工作集 | 不含 WebView2/Office，目标 < 80 MiB |
| Space 到窗口骨架可见 | P95 < 50 ms |
| 小型文本/常规图片首屏 | 本地 SSD，P95 < 150 ms |
| 选择变化到新内容骨架 | P95 < 50 ms |
| 过期内容提交 | 0 次 |
| 空闲独立窗口 | 未使用的 Provider 资源可回收，不持续占用 CPU |

WebView2、Office、FFmpeg 和大型媒体的进程及 GPU 资源单独统计，不能用共享运行时或驱动内存掩盖 Glance 自身泄漏。

## 18. 故障处理与恢复

| 故障 | 行为 |
| --- | --- |
| Core 启动失败 | UI 警告，预览入口禁用或尝试普通权限模式 |
| UAC 拒绝 | 普通权限运行，标记降级状态 |
| Hook 健康异常 | 先安装替代 Hook，再移除旧 Hook，记录诊断 |
| Explorer 重启 | 清空宿主缓存，重新枚举窗口与适配器 |
| IPC 断开 | Core 放行按键；UI 重连或重启 Core |
| UI 崩溃 | Core 检测心跳丢失并停止吞键，等待 UI 重启 |
| Provider 超时 | 取消会话，显示错误或回退 Generic |
| RenderHost 崩溃 | 当前请求失败，宿主重建，同文件熔断 |
| Office 无响应 | 取消导出，关闭 OfficeHost，不终止未知 Office 进程 |
| GPU 设备丢失 | 重建共享设备和视觉树，保留窗口状态 |
| 文件被删除/替换 | 关闭旧句柄结果，显示文件不可用并等待新选择 |

## 19. 依赖策略

### 19.1 系统依赖

- Win32、COM、Shell API、UI Automation；
- Windows App SDK、WinUI 3、C++/WinRT；
- Direct3D 11、DirectComposition、Direct2D、DirectWrite、WIC；
- Media Foundation；
- WebView2 Evergreen Runtime；
- Office COM，仅在本机安装相应 Office 时启用。

### 19.2 固定第三方依赖候选

| 依赖 | 用途 | 决策关注点 |
| --- | --- | --- |
| Scintilla + Lexilla | 文本、代码、语法高亮 | 轻量、原生 HWND、许可证 |
| PDFium | PDF 页面、目录、缩略图 | 二进制体积、更新频率、沙箱隔离 |
| libarchive | ZIP/7z/TAR/RAR 等只读目录 | 格式覆盖、安全更新、许可证 |
| cmark-gfm 或 md4c | Markdown 转 HTML | CommonMark/GFM 覆盖、HTML 安全 |
| uchardet 或等价实现 | 无 BOM 编码猜测 | 中文编码准确率、体积、线程安全 |
| FFmpeg/libav* | 媒体探测、解复用、软硬件解码 | 接受 GPL、硬件互操作、安全更新、体积 |

依赖进入仓库前必须完成许可证确认、版本固定、哈希校验、漏洞更新策略和最小功能裁剪。不得使用会在运行时联网下载组件的包。

当前开发机 PATH 中的 FFmpeg 是 2022-04-07 GPL full build，具有 D3D11VA、DXVA2、QSV、CUDA/NVDEC、OpenCL 和 Vulkan 能力，可用于阶段 0 对照实验；其版本、GPL 配置和静态构建不能直接作为发布依赖基线。

## 20. 源码组织建议

```text
Glance.sln
src/
  Glance.App/                 # UI 入口、窗口和会话管理
  Glance.Core/                # Hook、前台窗口、选择适配器
  Glance.Contracts/           # IPC schema、公共数据模型、错误码
  Glance.Platform/            # Win32/COM/DirectX RAII 封装
  Glance.Preview.Abstractions/# Provider 接口和调度器
  Glance.Preview.Text/
  Glance.Preview.Image/
  Glance.Preview.Media/
  Glance.Preview.Markdown/
  Glance.Preview.Generic/
  Glance.RenderHost/
  Glance.Preview.Pdf/
  Glance.Preview.Archive/
  Glance.OfficeHost/
  Glance.Preview.Office/
third_party/
assets/
packaging/
```

公共项目依赖必须单向：Provider 可以依赖 Abstractions 和 Platform，Core 不得依赖任何 Provider 或 UI 项目，Contracts 不得依赖 Windows UI。

## 21. 构建与部署

- 首期只构建 x64，降低 Hook、COM 和依赖矩阵复杂度；
- 使用 MSBuild 统一构建 WinUI/XAML 和 C++ 项目；
- Windows SDK 目标版本使用已安装的 10.0.26100，最小系统版本暂定 Windows 11 22000；
- Windows App SDK 使用稳定版并固定版本；
- Debug、Release 和可诊断 Release 配置分离；
- 安装器写入 Program Files、创建启动入口并部署必要运行时；
- 应用二进制最终需要 Authenticode 签名；
- Core 使用 `asInvoker` manifest，通过 UI 显式 `runas` 提升，从而支持 UAC 拒绝后的普通权限回退；
- 计划任务方案通过安装器注册为指定用户的 Logon Trigger、Interactive Token 和 Highest Run Level，Core 可执行文件必须位于仅管理员可写的安装目录并验证签名；
- 不注册 Explorer Shell Extension，不向 Explorer 进程注入 DLL。

## 22. 实施阶段与退出条件

### 阶段 0：技术风险原型

必须先验证：

1. 双 Hook、Raw Input 和重建策略在 Explorer 中的行为；
2. Windows 11 Explorer 标签页的 Shell COM 选择读取；
3. 原生文件对话框、Everything、Listary 的 UIA 结构；
4. 提权 Core 与普通权限 UI 的命名管道和 Shell COM 行为；
5. WinUI 3 非激活窗口、鼠标控件、滚轮、Scintilla 和 WebView2 托管；
6. OfficeHost 只读导出 PDF 且不影响用户 Office 实例；
7. PDFium 目录、缩略图和渐进渲染能力；
8. Media Foundation 与 FFmpeg 在常见及高码率媒体上的首帧、Seek、CPU/GPU、功耗和格式覆盖差异；
9. 登录触发计划任务启动的 Core 是否稳定处于正确交互桌面，以及失败重启、锁屏、解锁和多用户会话行为。

退出条件：所有核心风险有可重复结果，失败项已有明确替代方案，并据此冻结 ADR-003、ADR-005 和 PDF 后端。

### 阶段 1：可靠预览骨架

实现 Core/UI、IPC、活动窗口状态机、Explorer 选择、Space/Esc、Generic Provider、窗口尺寸与设置持久化。

退出条件：系统恢复、Explorer 重启、UI 重启、来源失焦和快速切换文件时行为确定，不吞掉非目标窗口的 Space。

### 阶段 2：基础 Provider

实现文本/代码、Markdown、图片、媒体和格式探测。完成取消、代次控制、元数据 overlay 和底栏动作。

### 阶段 3：多实例与隔离

实现钉住+置顶分离、多独立窗口、RenderHost、崩溃恢复和资源回收。

### 阶段 4：复杂格式

依次实现 PDF、压缩包和 Office。每个 Provider 独立完成安全限制、超时、降级和资源预算后再启用。3D 不纳入首版，后续扩展时优先评估 STL。

### 阶段 5：兼容性与发布

完成 Everything/Listary 版本矩阵、多显示器/DPI、休眠恢复、安装升级、签名和诊断导出。

## 23. 架构验证清单

后续验证应覆盖但不限于：

- 持续快速 Space/Esc、长按、键盘映射软件和多键盘设备；
- Explorer 各视图、标签页、桌面、搜索结果和多选；
- 文件对话框中文件名输入、搜索输入和文件列表之间切换；
- UAC 接受/拒绝、普通与高权限来源窗口；
- Explorer、UI、Core、RenderHost 分别崩溃或重启；
- 睡眠、休眠、锁屏、用户会话切换和远程桌面；
- 多显示器、不同 DPI、任务栏位置变化和显示器移除；
- 巨型文本、超大图片、高码率媒体、损坏 PDF、压缩炸弹和恶意路径；
- Office 已安装、未安装、首次启动、受保护视图和 COM 无响应；
- 快速方向键切换时无过期内容回跳、无句柄和 GPU 资源持续增长。

## 24. 已知风险与待评审问题

### 高风险

1. **Hook 的绝对可靠性不可由 Windows 用户态 API 保证。** 需要以可检测、冗余和自动恢复定义工程可靠性，而不能作形式化零失效承诺。
2. **WinUI 3 非激活窗口互操作。** WebView2、Scintilla、XAML 指针输入和原生子窗口可能存在焦点或层级限制，必须先做原型。
3. **选择快照存在时序窗口。** 事件晚到可能导致 Space 使用旧选择；必须用实测确定快照年龄策略，且不能在 Hook 回调内用同步 COM 查询掩盖问题。
4. **Explorer Shell COM 跨完整性级别。** 提权 Core 是否能稳定映射 Windows 11 标签页需实机验证，失败时需增加普通权限 Selection Agent 或使用 UIA 降级。
5. **高权限文件访问跨界。** Core 可见但 UI 无权读取的文件需要严格受限的句柄代理，否则会形成提权读取接口。
6. **Office COM 稳定性。** Office 的首次启动、弹窗、加载项和受保护视图不可完全控制，必须放在可取消的独立宿主。
7. **PDFium 更新成本。** PDF 是高风险解析面，需要持续跟随安全更新。

### 已确认产品决策

1. Pinned 状态不因来源窗口失焦自动关闭。
2. 独立实例取消钉住后不恢复选择监控，在下一次前台焦点变化或用户手动关闭时退出。
3. 密码 PDF 和加密压缩包不提供抢焦点的密码输入，直接显示不支持。
4. 接受 GPL 依赖和相应发布约束。
5. Office 仅支持 Word、Excel 和 PowerPoint。
6. 云文件只增加一次加载确认，之后按普通文件处理，失败直接显示错误。
7. 独立预览窗口不设置数量上限。

## 25. 参考资料

- [Windows App SDK](https://learn.microsoft.com/en-us/windows/apps/windows-app-sdk/)
- [LowLevelKeyboardProc](https://learn.microsoft.com/en-us/windows/win32/winmsg/lowlevelkeyboardproc)
- [Raw Input](https://learn.microsoft.com/en-us/windows/win32/inputdev/about-raw-input)
- [UI Automation Security](https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-securityoverview)
- [Direct2D Overview](https://learn.microsoft.com/en-us/windows/win32/direct2d/direct2d-overview)
- [Media Foundation Programming Guide](https://learn.microsoft.com/en-us/windows/win32/medfound/media-foundation-programming-guide)
- [WebView2 Distribution](https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/distribution)
- [Interactive Services](https://learn.microsoft.com/en-us/windows/win32/services/interactive-services)
- [Task Scheduler Logon Trigger](https://learn.microsoft.com/en-us/windows/win32/api/taskschd/nn-taskschd-ilogontrigger)
- [Scintilla](https://scintilla.org/)
