# 需求文档： Glance 一瞥

## 软件功能

软件需要提供在Windows下类似MacOS的空格预览体验，即在资源管理器窗口中，在选中文件后，按下空格键，可以预览文件内容。

当前开源/付费的类似软件很多，如Seer、QuickLook等，但都存在软件稳定性不足，空格Hook偶发失效的问题，本软件需要严格完成下面需求：

## 动作边界

在 Explorer.exe，windows原生文件选择窗口，以及兼容的Everything、Listary等文件搜索软件的文件选择窗口中，当且仅当选择一个或多个文件后，按下空格键，弹出文件内容预览窗口。

## 空格Hook

空格的Hook一定要严格可靠，预览功能必须表现的像系统原生功能一样可靠，任意一次按下空格但预览不生效都会给用户体验带来毁灭性的负面影响。

## 支持的文件类型

1. 常规文本文件，包括代码文件如 .txt .md .c .py ...，对于Markdown，需要提供右下角的Preview/Code切换按钮，切换作为代码预览和渲染结果预览；对于代码文件，需要提供语法高亮和行号显示；提供文件编码自适应，以及预览编码切换按钮。设置应提供文本预览器的行号显示、字号大小、字体、高亮风格等选项。
2. 常规图片文件，需要提供EXIF显示（如有），尺寸等元数据显示，图片zoom，pan，rotate操作，图片zoom到最小缩放时应该fit窗口，不允许更小了
3. 常规视频、音频媒体文件，提供播放控制，进度条，播放/暂停，音量调节等操作，并提供基础元数据显示，其中音量可在设置中设置是否启用默认值而非记忆，点击底栏音量按钮切换静音，悬浮弹出音量条
4. PDF预览，提供章节导航和页面缩略图导航；加密PDF不提供需要抢占焦点的密码输入，直接显示不支持状态
5. 压缩包内容预览；加密压缩包不提供需要抢占焦点的密码输入，直接显示不支持状态
6. Office预览，仅支持Word、Excel和PowerPoint，要求本机已有对应Office程序以减轻预览依赖
7. 提供格式猜测功能，对于未知格式，首先猜测是否是纯文本文件，其次按文件头猜测是否是某种媒体文件，随后才是提示格式不支持，按通用文件打开预览窗口；
8. 通用文件（即不支持文件）显示文件缩略图、文件大小、文件路径、修改时间等基础信息。

3D模型预览不纳入初版范围，后续扩展时优先支持STL格式，再评估其他3D格式。

虚拟文件和云占位文件首次拒绝直接预览，按通用文件显示基础信息，并提供按钮要求用户额外确认。确认后按普通文件执行一次载入，一般这时会拉起OneDrive等程序准备文件内容；载入失败时直接显示错误，不做额外的云文件适配。


## 软件操作逻辑

所有的文件类型可控按钮放置在底栏右下角，如编码、preview切换、元数据显示切换等。例外的是媒体播放控制栏，使用底部悬浮栏实现。底栏提供一个快速按钮复制文件路径（设置中可切换路径风格：是否带引号、是否使用Unix/分割）如果检测到不是Explorer.exe唤起的预览，额外显示打开所在文件夹和使用默认程序打开文件的按钮。

元数据等大量丰富的详细信息文本，在软件左上方使用半透明overlay来显示。

标题栏有文件名，置顶按钮和钉子按钮，逻辑见后。

底栏左下角包括文件基础信息：文件大小、最后修改时间等，具体显示信息可在设置中设置。

预览窗口允许通过窗口边缘和四角自由调整尺寸，并支持最大化、还原等Windows原生窗口操作。新建预览窗口时，应根据内容类型、内容固有尺寸、当前显示器工作区和DPI自动计算合适的初始尺寸，同时设置合理的最小尺寸和不超过当前显示器工作区的最大尺寸。图片、视频等具有固定宽高比的内容应在内容区域内保持比例并居中显示，不能因窗口尺寸变化而拉伸；文本、PDF、Office和压缩包等流式或分页内容按预设的合理阅读尺寸打开。

内容自适应仅负责窗口的初始尺寸。在用户手动调整窗口尺寸后，该窗口后续切换文件时保持用户设置的尺寸，避免窗口随文件选择变化反复跳动。可在设置中选择是否按预览类型记忆上次手动设置的窗口尺寸，并提供恢复内容自适应尺寸的窗口控制。

避免复杂快捷键，尽量使用鼠标和原生逻辑操作。如由于不抢占焦点，方向键、复制剪切、回车打开等操作都应由系统正常handle。

仅有的快捷键：Space/Esc-退出预览，滚轮-图片缩放（默认以鼠标位置为中心缩放，按住Ctrl以中心缩放）/媒体进度调节（按住Ctrl调节音量）/文本滚动（按住Ctrl缩放文本字号，记忆字号）

## 集成逻辑

软件采用核心单实例、预览窗口可多实例的模式。常驻核心进程始终只有一个，负责空格Hook、焦点和文件选择监控、实例管理；默认活动预览窗口同时最多只有一个。活动预览窗口默认不抢占焦点，并且记忆焦点软件，如果焦点改变视为文件选择窗口失去焦点，则退出预览。

由于弹出窗口后焦点依然在文件选择窗口，因此上下左右键依然可以改变所选文件，软件要持续监视文件选择变化并提供最新选中文件的预览。

当置顶后，软件不因失焦而自动退出，作为一个悬浮预览窗持续预览所选文件，允许跨窗口等切换焦点而不退出游览。

当钉住后，该窗口不再监控最新文件，而是持续提供最后一个文件的预览功能，并且不因来源窗口失焦而自动关闭。

当预览窗口同时处于钉住和置顶状态时，该窗口从当前文件选择会话中分离，转为独立持续预览实例。独立实例保持当前文件和自身的窗口尺寸、位置、预览状态，不再响应来源窗口的文件选择变化，也不占用默认活动预览窗口名额。此时继续在兼容的文件选择窗口中按下空格，可以创建新的活动预览窗口；任意数量的已分离独立实例可以与一个活动预览窗口同时存在，不设置实例数量上限。

全局Space/Esc只打开或关闭当前活动预览窗口，不关闭已分离的独立实例。独立实例可以通过自身标题栏关闭按钮关闭，其置顶状态可以独立切换。取消独立实例的钉住状态后不恢复文件选择监控，保留当前内容，随后在前台焦点再次变化时自动关闭，也允许通过标题栏手动关闭。

特殊的是，如果选择多个文件启动预览，在预览窗口左侧显示一个文件列表，包含所有选中的文件，默认对最后，也就是文件浏览器中的最后高亮文件进行预览。文件列表按选择顺序（原生顺序）排列，并高亮当前预览的文件，点击可切换预览。

## 软件性能/界面实现

软件一定要轻量高效，界面设计需要现代化，符合Win11的设计语言，并提供暗色模式和强调色调节。

对于特殊预览功能的实现，考虑使用Win11内置集成框架，如Edge Webviewer等实现，尽量不使用第三方库，以保证软件的轻量高效。

媒体预览允许使用FFmpeg及其库，并接受GPL依赖。Media Foundation和FFmpeg的主后端、硬件解码路径及降级顺序应通过实际性能、格式兼容性和稳定性测试决定，以性能优先；不得仅因FFmpeg已存在于系统PATH便形成运行时依赖，发布版本使用固定、可审计的依赖版本和构建配置。

软件以高性能，高可靠为最终目标，不实现任何运行时插件系统，仅在代码架构时考虑对后续新增的预览格式支持提供统一的模块化框架，但所有新增格式都通过源码编译实现，不提供任何运行时插件支持。

外部确定性依赖是允许的，如开源库，动态加载Office COM等，拒绝的是如python等“运行时插件系统”

软件强要求管理员权限运行，如果管理员权限被拒绝，则不保证对应功能正常生效，显示警告但尝试继续运行。
